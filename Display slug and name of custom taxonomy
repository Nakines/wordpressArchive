<?php   

  //Slug
    $magazine   = ts_get_the_category('magazine', 'big_array:1', '', $ts_query->post->ID);
    $magazine = esc_html($magazine[0]['slug']); 

  //Name
    $magazine_name = ts_get_the_category( ( 'magazine' ) );
                  
                ?>
